# Server Configuration
# Karena web terinstall di tiap PC ruangan sebagai bagian dari Room App,
# SERVER_IP harus = IP PC ruangan itu sendiri (tempat server jalan).
VITE_SERVER_IP=127.0.0.1
VITE_SERVER_PORT=53331

# Billing Configuration
VITE_BILLING_ENABLED=true
